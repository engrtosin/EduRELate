<?xml version="1.0" encoding="utf-8"?>
<!-- fragment_new_group.xml -->
<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/new_group"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_gravity="center"
    android:padding="15dp"
    android:orientation="vertical"  >

    <TextView
        android:id="@+id/lblNewPic"
        android:text="New Picture"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textStyle="bold"/>

    <ImageView
        android:id="@+id/ivNewPic"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:maxWidth="100dp"
        android:maxHeight="100dp"
        android:scaleType="fitXY"
        android:layout_below="@id/lblNewPic"
        android:layout_marginStart="92dp"
        android:layout_marginTop="5dp"
        tools:srcCompat="@tools:sample/avatars" />

    <ImageView
        android:id="@+id/ivDoneBtn"
        android:layout_width="30dp"
        android:layout_height="30dp"
        android:layout_alignParentEnd="true"
        android:layout_below="@id/ivNewPic"
        app:srcCompat="@drawable/ic_baseline_done_24" />

    <ImageView
        android:id="@+id/ivCameraBtn"
        android:layout_width="30dp"
        android:layout_height="30dp"
        android:scaleType="fitXY"
        android:scaleX="2"
        android:scaleY="2"
        android:layout_below="@id/ivNewPic"
        android:layout_alignParentStart="true"
        app:tint="@color/black"
        app:srcCompat="@drawable/camera_shadow_fill" />

    <ImageView
        android:id="@+id/ivDocBtn"
        android:layout_width="30dp"
        android:layout_height="30dp"
        android:layout_alignTop="@id/ivCameraBtn"
        android:layout_marginStart="5dp"
        android:layout_toEndOf="@id/ivCameraBtn"
        app:tint="@color/black"
        app:srcCompat="@drawable/ic_baseline_attach_file_24" />
</RelativeLayout>